{
  "name": "client",
  "version": "1.0.0",
  "description": "Client to make requests to HTTP server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Paavo Hemmo",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.19.2"
  }
}
